# rails plugin new china_university --dummy-path=spec/dummy --skip-test-unit --mountable
if ENV['TRAVIS']
  source 'https://rubygems.org'
else
  source 'http://ruby.taobao.org'
end

gemspec
gem 'jquery-rails'
